using UnityEngine;

public class StructRefShop : MonoBehaviour
{
    //A simple script that will be updated when player can select there strucuture 
    // To link each shop button to a structure
    public Structure structure;  // This will hold the reference to the Structure ScriptableObject
}